# Маршрутка — только UI. Ни секретов каналов, ни базы здесь нет.

# Адрес youtube-pipelines API. Он стоит на сервере с конвейерами.
HP_API_URL=http://141.136.44.9:8010
HP_API_TOKEN=ТОТ_ЖЕ_ТОКЕН_ЧТО_HP_WEB_TOKEN

# Порт UI. Диапазон UI по конвенции — 8100-8199.
UI_PORT=8100

# Пин-код входа на / (публичный лендинг) -> /login. Не про Google OAuth —
# отдельный, независимый гейт для самой панели.
AUTH_PIN=080887

# Секрет для подписи сессионной куки (HMAC-SHA256). Сгенерировать:
# python3 -c "import secrets; print(secrets.token_hex(32))"
AUTH_SECRET=
